product: '1.0.0'

info:
  name: server
  title: server
  version: "1.0.0"

apis:
  'server':
    $ref: server.yaml

visibility:
  view:
    type: public
  subscribe:
    type: authenticated

plans:
  default:
    title: Default Plan
    description: Default Plan
    approval: false
    rate-limit:
      value: 100/hour
      hard-limit: false
